/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  experimental: {
    appDir: true, // keep this if you're using the /app folder
  },
};

export default nextConfig;
